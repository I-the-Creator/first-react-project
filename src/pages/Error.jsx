import React from 'react';

const Error = () => {
    return (
        <div>
            <h1 style={{color: 'red'}}>
                You're tying to open non-existing page!
            </h1>
        </div>
    );
};

export default Error;